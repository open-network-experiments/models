components:
  schemas:
    Server:
      x-include: 
      - '../../common/common.yaml#/components/schemas/GlobalNamedObject'
      properties:
        mac_address:
          description: |-
            The mac address
          type: string
          format: mac
