components:
  schemas:
    Config:
      type: object
      properties:
        hosts:
          type: array
          items:
            $ref: "../datamodel/host/host.yaml#/components/schemas/Host"
        fabric:
            $ref: "../datamodel/fabric/fabric.yaml#/components/schemas/Fabric"
