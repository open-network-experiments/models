components:
  schemas:
    Metrics.Request:
      type: object
      properties:
        port_metrics:
          $ref: "./port_metrics.yaml#/components/schemas/PortMetrics.Request"

    Metrics.Response:
      type: object
      properties:
        port_metrics:
          $ref: "./port_metrics.yaml#/components/schemas/PortMetrics.Response"
