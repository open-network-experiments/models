components:
  schemas:
    Profiles:
      description: |-
        Key where all the profiles are stored
      type: object
      properties:
        eth_nic_profiles:
          type: array
          items:
            $ref: './eth_nic_settings.yaml#/components/schemas/Profiles.Dataflow.HostManagement.EthNicSetting'